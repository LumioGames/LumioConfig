// <auto-generated>
// 生成物不得手改。由 LumioConfig `export --csharp-out` 重建。
// table: drops
// target: V
// schemaFingerprint: 8cd292d062c1abe5f497ed58c9aac35258301ce048ae1c6b58e0f2d00e5f50cc
// </auto-generated>

#nullable enable

using System;
using System.Collections.Generic;

namespace Lumio.Config.Generated.Voxel;

public readonly struct DropsRow
{
    public DropsRow(
        uint id,
        string name)
    {
        Id = id;
        Name = name;
    }

    public uint Id { get; }
    public string Name { get; }
}

public readonly struct DropsTable
{
    private readonly DropsRow[] _rows;
    private readonly Dictionary<uint, int> _index;

    public DropsTable(IReadOnlyList<DropsRow> rows)
    {
        if (rows is null)
        {
            throw new ArgumentNullException(nameof(rows));
        }

        var copy = new DropsRow[rows.Count];
        for (var i = 0; i < rows.Count; i++)
        {
            copy[i] = rows[i];
        }

        Array.Sort(copy, static (left, right) => left.Id.CompareTo(right.Id));
        var index = new Dictionary<uint, int>(copy.Length);
        for (var i = 0; i < copy.Length; i++)
        {
            var id = copy[i].Id;
            if (!index.TryAdd(id, i))
            {
                throw new ArgumentException($"duplicate id {id}", nameof(rows));
            }
        }

        _rows = copy;
        _index = index;
    }

    public int Count => _rows.Length;

    public IReadOnlyList<DropsRow> Rows => _rows;

    public bool TryGet(uint id, out DropsRow row)
    {
        if (_index.TryGetValue(id, out var offset))
        {
            row = _rows[offset];
            return true;
        }

        row = default;
        return false;
    }
}
