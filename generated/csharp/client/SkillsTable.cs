// <auto-generated>
// 生成物不得手改。由 LumioConfig `export --csharp-out` 重建。
// table: skills
// target: C
// schemaFingerprint: 54c79f960e25814e5aa0c1b84bd6157aee7e8022b422fb6e5d19fb19dfd9be62
// </auto-generated>

#nullable enable

using System;
using System.Collections.Generic;

namespace Lumio.Config.Generated.Client;

public readonly struct SkillsRow
{
    public SkillsRow(
        uint id,
        string name,
        string displayName,
        string icon)
    {
        Id = id;
        Name = name;
        DisplayName = displayName;
        Icon = icon;
    }

    public uint Id { get; }
    public string Name { get; }
    public string DisplayName { get; }
    public string Icon { get; }
}

public readonly struct SkillsTable
{
    private readonly SkillsRow[] _rows;
    private readonly Dictionary<uint, int> _index;

    public SkillsTable(IReadOnlyList<SkillsRow> rows)
    {
        if (rows is null)
        {
            throw new ArgumentNullException(nameof(rows));
        }

        var copy = new SkillsRow[rows.Count];
        for (var i = 0; i < rows.Count; i++)
        {
            copy[i] = rows[i];
        }

        Array.Sort(copy, static (left, right) => left.Id.CompareTo(right.Id));
        var index = new Dictionary<uint, int>(copy.Length);
        for (var i = 0; i < copy.Length; i++)
        {
            var id = copy[i].Id;
            if (!index.TryAdd(id, i))
            {
                throw new ArgumentException($"duplicate id {id}", nameof(rows));
            }
        }

        _rows = copy;
        _index = index;
    }

    public int Count => _rows.Length;

    public IReadOnlyList<SkillsRow> Rows => _rows;

    public bool TryGet(uint id, out SkillsRow row)
    {
        if (_index.TryGetValue(id, out var offset))
        {
            row = _rows[offset];
            return true;
        }

        row = default;
        return false;
    }
}
