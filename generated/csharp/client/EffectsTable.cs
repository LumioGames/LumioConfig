// <auto-generated>
// 生成物不得手改。由 LumioConfig `export --csharp-out` 重建。
// table: effects
// target: C
// schemaFingerprint: c1588021f668cb15e51ed81461e19fe8287b66df4d646db888873929912c02d1
// </auto-generated>

#nullable enable

using System;
using System.Collections.Generic;

namespace Lumio.Config.Generated.Client;

public readonly struct EffectsRow
{
    public EffectsRow(
        uint id,
        string name,
        string fxKey)
    {
        Id = id;
        Name = name;
        FxKey = fxKey;
    }

    public uint Id { get; }
    public string Name { get; }
    public string FxKey { get; }
}

public readonly struct EffectsTable
{
    private readonly EffectsRow[] _rows;
    private readonly Dictionary<uint, int> _index;

    public EffectsTable(IReadOnlyList<EffectsRow> rows)
    {
        if (rows is null)
        {
            throw new ArgumentNullException(nameof(rows));
        }

        var copy = new EffectsRow[rows.Count];
        for (var i = 0; i < rows.Count; i++)
        {
            copy[i] = rows[i];
        }

        Array.Sort(copy, static (left, right) => left.Id.CompareTo(right.Id));
        var index = new Dictionary<uint, int>(copy.Length);
        for (var i = 0; i < copy.Length; i++)
        {
            var id = copy[i].Id;
            if (!index.TryAdd(id, i))
            {
                throw new ArgumentException($"duplicate id {id}", nameof(rows));
            }
        }

        _rows = copy;
        _index = index;
    }

    public int Count => _rows.Length;

    public IReadOnlyList<EffectsRow> Rows => _rows;

    public bool TryGet(uint id, out EffectsRow row)
    {
        if (_index.TryGetValue(id, out var offset))
        {
            row = _rows[offset];
            return true;
        }

        row = default;
        return false;
    }
}
