# 系统规则

- 必须把 tables/、schemas/ 和 registry/ 视为权威源；不得把生成物反向当作源。
- 不得手改导出产物；只能通过源文件和工具命令重建。
- 不得在表中加入 if、脚本、蓝图式逻辑或隐式运行时行为。
- 不得把密钥、生产数据、用户数据或真实未公开业务数据提交到仓库。
- 不得让 AI 或 CLI 直接激活生产 Revision；生产激活必须经过人类 Owner 门。
- 影响跨仓公共契约的改动必须先回架构仓走 ADR 和契约校验流程。
- 对外 push、发布、改访问控制或生产操作前必须获得明确授权。
