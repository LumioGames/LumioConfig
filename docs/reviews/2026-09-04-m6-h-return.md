# M6-H(R-00380)交回物 · 2026-09-03

> **移档说明(2026-09-04)**:本文为 M6 收口时当场写下的原始交回物,因 `.sdd/` 整目录 gitignore(`*`)未曾入库;2026-09-04 按 M7-J(R-00402,Owner 闸门选项 A)从本地 `.sdd/` 移入 `docs/reviews/` 留档。原文内容未改写,仅文末追加「2026-09-04 审计对账补注」。原件提及的 `.sdd/` 台账(gate 日志、审查报告、diff 包)仍未入库,仅存撰写机器本地。

集成分支 `feat/editor-v3-m6-h`(基线 a63f287),合入 main 提交 `7565a9a`,已 push。

## ① 改动清单

| 文件 | 一句话 |
| --- | --- |
| panels/{TopBar,Banner}.tsx(新)、StatusBar.tsx(重写) | 42px 顶栏(修订退化/胶囊/按钮 disabledReason/⋯ 菜单)、§5 横幅(draft-refresh 动作)、24px 状态条(指纹 8 位复制、aria-live 区) |
| panels/{TableList,GridToolbar,Inspector}.tsx(新/重写) | 表列表(搜索/徽标/44px 折叠/onboarding toast)、白名单工具栏、只读检查器(§7 段序、四态键带禁用原因) |
| spreadsheet/{projection,cellMeta,viewState,badges}.ts | STYLES 语义色、两行列头、脏/新/删/只读/无效标记、ADR 0008 渲染徽标单一安装点、rowCount=rows+3、invalidReason、inspectorOpen/sidebarCollapsed |
| app/App.tsx、styles/app.css | 主 loop 壳接线:壳网格 42/auto/1fr/auto/24、六组件挂载、Ctrl+M/选格展开检查器、Failed·VCS failKind 产生点、debugPhase 注入桥、fixture 表清单、组件 CSS 全套 |
| spreadsheet/editors.ts、components/ui/useHotkeys.ts | DV 挂载行截到 rows+3;热键大小写归一(P2-1) |
| tests/{TopBar,Banner,StatusBar,TableList,GridToolbar,Inspector}.test.tsx(新)、viewState/projection.roundtrip 扩展、e2e/layout.spec.ts(新) | 组件单测 + 布局/检查器/14 态验收 |
| e2e/{keyboard,four-state}.spec.ts | 36px 列头坐标校准、live region 空串、无未提交改动文案、可见菜单作用域 |
| src/lumio_config/editor_static/ | 随卡重建 ×3,幂等 |

## ② 验证证据

- 门槛(终态):lint+check-deps、vitest **226/226**、build、**e2e 42/42(1.1m)**、python 141 OK、validate/format/diff-check、editor_static 幂等复验。
- 快审 + 复审(`.sdd/m6-h-review.md`):首轮 RETURN 3×P1(横幅 200px 落位 / 侧栏+工具栏 CSS 整套缺失 / remoteErrors 未按行过滤),修复后复审 **MERGE**——三条实测通过(banner 全宽 1440、徽标 computed 色 = --color-dirty 系、行过滤落地);P2-1/P2-2 已随返工修复(uncommittedMerges 真值、指纹剥前缀),P2-3(删除行重投影)→ M6-I、P2-5(TopBar/StatusBar 游离文案补 COPY 键)→ M6-J。

## ③ known gaps

- P2-3:检查器删除行/撤销删除无即时重投影(需重挂工作簿,M6-I 接 onJump 时处理)。
- P2-5:TopBar/StatusBar 少量短文案游离于 copy.ts(导出/预检/在线等),M6-J 补键。
- 旧面板(DiffPreview/ConflictPanel/ExportPanel/SettingsPanel/ErrorPanel)仍在 .app-legacy 限高条带,随 M6-I 抽屉接线删除(按任务书 §3)。
- fixture 模式侧栏行数显示 0(无 /api/session,开表后才知道;host 模式正常)。

## ④ 知识沉淀落点

- 接线排障 7 条全录 `.sdd/progress.md`(ToastProvider 崩溃、右键选区时序、persistView 覆写竞态、contenteditable 误判、右键不展开检查器、fixture 表清单、坐标偏移);无新决策(壳布局按 ADR 0003 执行)。

## 附:Workflow 卡对账

- 卡号:R-00380。验收项证据:S01 → layout.spec 高度/检查器 6 例 + TableList/GridToolbar 单测;S02 → TopBar/Banner/StatusBar 单测 + host-drafts;S03 → projection.roundtrip 19 例 + badges.ts + ADR 0008;S04 → layout.spec 14 态注入。
- 台账:`.sdd/progress.md`;审查报告 `.sdd/m6-h-review.md`(含复审节)。
- Owner 动作:R-00380 流转验收。

---

## 2026-09-04 审计对账补注(移档时追加,非原件内容)

来源:`docs/reviews/2026-09-04-editor-v3-completion-audit.md`(下称「审计」,本仓提交 472f3f8;本节行号均指该版),审计对象 main@f2b0ecf,取证口径为审计员亲自复跑。

- 审计 §A:Task 11(行 61)「完成」、Task 13(行 63)「完成」、Task 12(行 62)「**部分**」——功能实走全部可用(搜索框、行数、脏格徽标 [1]、折叠、10 个工具栏按钮),但 `tests/TableList.test.tsx`(11)与 `tests/GridToolbar.test.tsx`(13)在审计机 Node v26.4.0 下 24 条全红。
- 审计 §B R-00380(行 90–93):S01「部分」(侧栏单测本机全红)、S02「部分」(工具栏单测本机全红,功能已实走确认)、S03「完成」(`projection.roundtrip` 19 条 + 实走脏格/新行占位/两行列头/只读锁)、S04「完成」(`layout.spec.ts:98` 全相位断言)。
- **环境保留意见(审计 §G-2)**:Node 26 自带全局 `localStorage` 访问器,遮蔽 vitest jsdom 的 Storage——本卡侧栏/工具栏的「单测绿」验收证据在当前工具链上取不到,且任何升到 Node 26 的机器 `pnpm test` 都会红;审计判定为环境冲突、非产品代码缺陷(功能浏览器实走确认可用)。修复立卡 M7-K(R-00403,需求卡 `.spec/plans/2026-09-04-editor-v3-hardening-requirements.md` 附录 B G-2)。
- 布局与检查器另由 `layout.spec` 5 条绿 + 实走背书(审计 §A Task 13,行 63:两档分辨率主区 ≥ 0.75、检查器七段齐全)。
- 未复核:原件 ② 引用的 `.sdd/m6-h-review.md` 与首轮 RETURN 三条 P1 的复审细节审计未读取(其 §0.2 / §G-4 按 git 视角判定交回物缺失,本补注不为其背书);原 known gaps(P2-3 删除行重投影、P2-5 游离文案等)未逐条复核,以原件记录为准。
