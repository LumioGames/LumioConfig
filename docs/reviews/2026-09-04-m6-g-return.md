# M6-G(R-00379)交回物 · 2026-09-03

> **移档说明(2026-09-04)**:本文为 M6 收口时当场写下的原始交回物,因 `.sdd/` 整目录 gitignore(`*`)未曾入库;2026-09-04 按 M7-J(R-00402,Owner 闸门选项 A)从本地 `.sdd/` 移入 `docs/reviews/` 留档。原文内容未改写,仅文末追加「2026-09-04 审计对账补注」。原件提及的 `.sdd/` 台账(gate 日志、审查报告、diff 包)仍未入库,仅存撰写机器本地。

集成分支 `feat/editor-v3-m6-g`(基线 0fe95bc,含 M6-F 同步),合入 main 提交 `49c14fc` + `a63f287`,已 push。

## ① 改动清单

| 文件 | 一句话 |
| --- | --- |
| editor/src/styles/{tokens,ui}.css | 38 令牌逐值落 §4 表(13 旧名保留、7 改值、12 新增、无 --color-danger);ui.css 弹层类名契约 + 接线补 tabs tone / menu is-active |
| editor/index.html | 标题 `LumioConfig` |
| editor/tests/no-hardcoded-colors.test.ts(新) | panels/components 不写字面色守卫 |
| editor/src/app/{phaseView,copy}.ts(新) | §5 状态表派生层 + §12/§5/§8 文案唯一来源(正则守卫) |
| editor/src/app/state.ts、api/types.ts | `failKind` 字段与动作归类;canRefreshOnly 按 failKind;canValidate 增 dirtyCount |
| editor/src/components/ui/{Tabs,Dialog,Toast,Menu,useHotkeys}(新)+ Button/index | 四原语 + 热键钩子;Button 增 disabledReason |
| editor/src/spreadsheet/{commands,menus}.ts(新)、univer.ts、interceptors.ts | COMMAND 白名单抽出(re-export);表面裁剪(toolbar/footer/隐藏表/快捷键中和/CSS 兜底);四态原生右键注入 |
| editor/docs/univer-surface.md(新) | preset 签名核实(d.ts 出处)与注入通道记录 |
| editor/src/app/App.tsx | 主 loop 接线:四态 handlers+availability 传 createSheetsUniver、删 four-state-menu/onContextMenu、消费 phaseView/COPY、409→failKind |
| editor/src/spreadsheet/menus.ts | stamp 轮询按最新 availability 补盖禁用态(右键选区晚于菜单构建) |
| editor/tests/e2e/{sheet-ops,four-state,host-drafts}.spec.ts | 三处禁止项断言;原生右键用例启用并按几何模型校准 |
| src/lumio_config/editor_static/ | 随卡重建(合并冲突以重建归一,幂等复验 0 diff) |

## ② 验证证据

- 全量门槛(`.sdd/m6-g-gate.log`):vitest 22 文件/147 条;e2e **36/36**(four-state 11/11 含原生右键;sheet-ops 9 含裁剪 3 例);python unittest OK;validate/format/diff-check 过;editor_static 幂等。
- 快审(`.sdd/m6-g-review.md`,MERGE,无 P0/P1):S01–S04 逐项通过;47 条契约单测 + 20 条 e2e 亲跑复核;lockfile 零变更、桥与 8 testid、6 中文断言片段全在。

## ③ known gaps(快审 P2)

- P2-1:`useHotkeys.ts:14` 未做按键大小写归一(真实浏览器 Ctrl+S 是小写 `s`)→ Task 17(M6-J)接字母键前必须修。
- P2-2:`failKindFromCode` 尚无 App 产生点,§5 的 Failed·VCS 行暂不可达 → D/E 阵列接线横幅时给 failed 产生点补归类。
- P2-3:poc-benchmark.md 漂移已还原;`.lumio/gui-test-screenshots/` 为会话前已存在的未跟踪目录,未动。
- 接线校准记录:右键选区更新晚于菜单构建→stamp 补盖;B4 盲写坐标落 fixture 空行→按 keyboard.spec 几何模型校准(台账有录)。

## ④ 知识沉淀落点

- 纯契约与接线,无新决策;ADR 0008 属 M6-F。univer-surface.md 记录 `_injector` 接缝为升版回归点。

## 附:Workflow 卡对账

- 卡号:R-00379(M6-G)。验收项证据:S01 → tokens.css/no-hardcoded-colors.test.ts;S02 → phaseView/copy/state 测试 + App 接线;S03 → Tabs/Dialog/Toast/Menu/useHotkeys 测试;S04 → univer-surface.md + sheet-ops 裁剪 3 例 + four-state 原生右键用例。
- 台账:`.sdd/progress.md`;快审报告 `.sdd/m6-g-review.md`。
- Owner 动作:R-00379 流转验收。

---

## 2026-09-04 审计对账补注(移档时追加,非原件内容)

来源:`docs/reviews/2026-09-04-editor-v3-completion-audit.md`(下称「审计」,本仓提交 472f3f8;本节行号均指该版),审计对象 main@f2b0ecf,取证口径为审计员亲自复跑。

- 审计 §A Task 5/6/7(行 55–57)与 Task 10(行 60)均判「完成」:`no-hardcoded-colors.test` 2 条绿 + 独立 grep `#[0-9a-fA-F]{3,8}\b|rgba?\(` 于 `panels`/`components` **0 命中**;`state`(23)+ `phaseView`(18)+ `copy`(4)全绿,`copy.ts` 11 处英文串全在注释/形参名/键名;Tabs(8)/Dialog(7)/Toast(4)/Menu(11)/useHotkeys(8)/Button(6) 单测全绿;`sheet-ops.spec` 6 条含「v3 surface trim」三测、`four-state.spec.ts:156` 原生右键绿。
- 审计 §B R-00379(行 86–89):S01–S04 四项均「完成」。
- 原 known gap P2-1(useHotkeys 按键大小写归一)与 P2-2(Failed·VCS failKind 产生点)已随 M6-H 消化(M6-H 原件改动清单「热键大小写归一(P2-1)」「Failed·VCS failKind 产生点」,档见 `2026-09-04-m6-h-return.md`)。
- 未复核:原件 ② 引用的 `.sdd/m6-g-gate.log`、`.sdd/m6-g-review.md` 审计未读取(其 §0.2 / §G-4 按 git 视角判定交回物缺失,本补注不为其背书);原 P2-3 等观察项未逐条复核,以原件记录为准。
