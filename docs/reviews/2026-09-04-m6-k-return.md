# M6-K(R-00383)+ Task 20(F3)交回物 · 2026-09-03

> **移档说明(2026-09-04)**:本文为 M6 收口时当场写下的原始交回物,因 `.sdd/` 整目录 gitignore(`*`)未曾入库;2026-09-04 按 M7-J(R-00402,Owner 闸门选项 A)从本地 `.sdd/` 移入 `docs/reviews/` 留档。原文内容未改写,仅文末追加「2026-09-04 审计对账补注」。原件提及的 `.sdd/` 台账(评审报告、像素探针记录)仍未入库,仅存撰写机器本地。

- 卡:R-00383「改动页签(修订级 diff / history)」(第 6 张也是最后一张 Workflow 卡)+ Task 20 docs-and-static 收口
- 分支:`feat/editor-v3-m6-k` @ c08991a(67c6219 同步 main → 1a33f43 口径回滚 → ec9b00b P1 修复 → c08991a P1-1R 修复)
- 合入:main `f2b0ecf`(merge --no-ff),已 push origin/main
- 评审:`.sdd/m6-k-review.md` —— 快审 **RETURN**(P1-1 rowId 跳格链失效 + P2×2)→ 修复 ec9b00b →
  复审 **RETURN**(P1-1R:命令 id 不存在静默失败、新断言中间层假绿; adversarial 像素探针实锤)→
  修复 c08991a(`sheet.command.select-range` 真命令 + `activeSelection` 终层断言)→ 二次复审 **MERGE**
  (三层证据互证:像素 1215 变化像素选区蓝 / Univer 选区状态读回 / lastJump 条件记录)

## 改动清单(cdacdc0..c08991a,30 文件 +1640/−210)

- python:`editor/vcs.py` +55(`git log`/`git show` 白名单命令、只列触及表路径)、
  `editor/history.py` +131(`GET /api/tables/{t}/history`、两修订快照按稳定 id 逐格比对、
  占位名 token 回退 row_id)、`editor/session.py`(capabilities.history);
  `tests/test_editor_history.py` +308(5 例)、`tests/test_editor_vcs.py` +140(9 例)
- 前端:`panels/drawer/DiffTab.tsx` 182(「我的/AI」分组、schemaChanged、rowId 跳格传参)、
  `api/client.ts`/`api/types.ts`(history 拉取、cells[].row 正型 string)、
  `app/App.tsx` +97(DiffTab 挂载、fetchHistory、myChanges() 派生、跳格三兜
  [数字行号→rowId 直命中→行名反查]、jumpToCell 真命令 select-range、
  PocBridge.lastJump/activeSelection)
- Task 20:`docs/reference/editor.md`(v3 界面段、快捷键表、「改动」页签 endpoint+jsonc 示例、
  错误码速查含 BAD_LIMIT/422)、`editor/docs/a11y-checklist.md`(24 项勾选,证据到用例 ID;
  a11y 三态 violations=[] 含 moderate 清零:h1/role=main/抽屉 section 标签/工具栏 region/
  全局 :focus-visible/删 hint 豁免)、`editor/docs/screens/` 四截图
- 静态包:fc12573 重建(Task 20)、c08991a 内含重建(P1 修复),均二次构建零 diff

## 验证证据

- 主 loop 亲跑(m6-k 各节点 + main@f2b0ecf 终局):lint(eslint+tsc+check-deps)过;
  vitest 313/313;全量 e2e 54 passed ×3 轮;python 150 OK(终局首跑与 e2e 同壳连跑出现
  1 次环境性 error,立即复跑 ×2 均 150 OK 不可复现;另两次全量 [1a33f43 主 loop、
  c08991a 评审] 亦 OK);validate/format --check/git diff --check 过;静态复算性过
- 评审三轮各自 detached worktree 亲跑:同套门槛全绿 + 浏览器像素探针
  (修复前 0 变化像素 vs 修复后 1215 选区蓝像素,阳性对照有效)
- 硬红线:零新增依赖、无字面色(tokens 守卫过)、ADR/schemas/tables/registry/
  repository.yaml 零改动、8 保留 testid 全在、文件集边界内(poc-benchmark.md 曾误提交,
  1a33f43 按 §6 回滚)

## Known gaps

1. P3-3:DiffTab 基准下拉与「在表格中标记」复选框渲染但 no-op(声明式保留,§8 后续需求)
2. P3-4:`vcs.py` since 参数理论注入面(`-` 开头可被 git 解析为选项;本地单用户 + token 下低危,建议后续校验 40-hex/refname)
3. 错误跳格 / J1 / J3 无独立 e2e(a11y 清单已标注;错误页签跳格随 c08991a 一并带活但无专测)
4. 灰度视觉走查 ×2、Blocked 真浏览器视觉走查未做(a11y 清单残留项)
5. 10 万格 TSV 粘贴尝试窗口 ~42s(v3 拦截管线下;POC 期 6.6s)——benchmark 文档按 §6 不提交,数字记档于此
6. 存量事实:补丁/错误/冲突页签跳格自 M6-H 起因 `sheet.command.set-selection` 命令不存在
   而静默失效,历届 e2e 未断言选区效果故漏网;c08991a 换真命令后一并带活,除 host-history 外建议后续卡补独立 e2e

## 沉淀(候选入 .spec/knowledge/,决策无新增 ADR)

- **executeCommand 静默失败陷阱**:Univer 0.25.1 命令 id 不存在不抛错、resolve 假值;
  e2e 断言必须落在终层(facade 读回真实选区 / 像素探针),lastJump 类中间层标记会假绿
- **真实选区命令**:`sheet.command.select-range` {unitId, subUnit, range, reveal}
  (subUnit=sheet.getSheetId());包内不存在 `sheet.command.set-selection`
- **rowKey 域纪律**:ProjectionMap.rowKeys 全是 id 字符串;Host history 响应带 rowId(同域),
  行名只做展示与 name.raw 反查兜底;类型上 cells[].row 正型 string

## 卡对账(R-00383 验收口径)

- S01 python vcs 白名单 history 能力 ✅ test_editor_vcs 9 例(白名单不越界,git show 路径服务端拼接)
- S02 Host history endpoint + capabilities.history ✅ test_editor_history 5 例 +
  server 注册(since/limit 语义、404/422、svn/none 空且 capability=false)
- S03 前端 DiffTab 接线与跳格 ✅ 修复后三层证据;host-history 4/4 含 activeSelection 终层断言
- S04 文档段 ✅ editor.md「改动」页签段(endpoint/参数/示例/BAD_LIMIT)+ v3 界面段/快捷键表/错误码速查

Task 20 对账:editor.md ✅、screens ×4 ✅、a11y 清单逐条勾选 ✅、静态重建复算 ✅、
M6-J/M6-K 两份 §8 交回物 ✅(本文件 + m6-j-return.md)。

---

## 2026-09-04 审计对账补注(移档时追加,非原件内容)

来源:`docs/reviews/2026-09-04-editor-v3-completion-audit.md`(下称「审计」,本仓提交 472f3f8;本节行号均指该版),审计对象 main@f2b0ecf,取证口径为审计员亲自复跑。

- 审计 §A:Task 8/9(行 58–59)「完成」(Python 150 条全绿;`session.py:182` `"history": self.settings.vcs == "git"`);Task 19(行 69)「完成」(`DiffTab` 8 条 + `host-history` 4 条绿,含 svn/none 下页签不出现);Task 20(行 70)「完成」(`editor.md` 127 行含「改动」页签/快捷键 v3/错误码速查、4 张截图、`pnpm build` 后 `editor_static` 零差异)。
- 审计 §B R-00383(行 102–105):S01–S04 四项均「完成」。
- 审计实走补充证据(§C 第 6 步,行 120):改动页签真列出修订 `691c2a98… feat(m1): pin column ordinal…` 与「表结构已变化」;基准下拉「上次打开」、「在表格中标记」开关、我的(1)`damage · 120 → null` 琥珀 chip、AI(3) 修订组。
- 原 known gap 3(错误跳格无独立 e2e):审计实走已补真浏览器人工证据(§C 第 5 步,行 119,点击跳格生效);独立 e2e 由 M7-B S04 补上(需求卡 `.spec/plans/2026-09-04-editor-v3-hardening-requirements.md` 附录 B E-4)。
- 原 known gap 6(存量 `set-selection` 静默失效史)与 P3-4(`vcs.py` since 理论注入面)审计未复核,以原件记录为准。
- 未复核:原件 ② 的三轮 detached worktree 亲跑与像素探针数值(1215 选区蓝像素等)审计未读取(其 §0.2 / §G-4 按 git 视角判定交回物缺失,本补注不为其背书)。
