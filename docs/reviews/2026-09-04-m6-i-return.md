# M6-I(R-00381)交回物 · 2026-09-03

> **移档说明(2026-09-04)**:本文为 M6 收口时当场写下的原始交回物,因 `.sdd/` 整目录 gitignore(`*`)未曾入库;2026-09-04 按 M7-J(R-00402,Owner 闸门选项 A)从本地 `.sdd/` 移入 `docs/reviews/` 留档。原文内容未改写,仅文末追加「2026-09-04 审计对账补注」。原件提及的 `.sdd/` 台账(gate 日志、审查报告、diff 包)仍未入库,仅存撰写机器本地。

集成分支 `feat/editor-v3-m6-i`(基线 7565a9a),合入 main 提交 `015ad04`,已 push。

## ① 改动清单

| 文件 | 一句话 |
| --- | --- |
| panels/drawer/{Drawer,PatchTab,ErrorTab,ConflictTab,ExportTab}.tsx(新) | 抽屉壳(30/240px、Ctrl+J、Esc 分层)+ 补丁/错误/冲突/导出四页签(签名逐字) |
| panels/SettingsDialog.tsx(新) | 两项提交设置 + 保存 toast,界面无 autoCommit/local.json |
| spreadsheet/viewState.ts | readSeen/writeSeen/changedSinceSeen,键 lumio-config-editor:seen:<repo>:<table> |
| app/{copy,state…经 App}.tsx、app.css | 主 loop 接线:抽屉挂载、SettingsDialog、seen 横幅+ack、Conflicted 自动切页签、冲突 resolved 记录语义、resubmit 重建补丁(patchRef 置空)+「已合入仓库 N 处改动」、跳格(行名→行键)、删五个旧面板、submit 后 banner 无误报回归断言 |
| tests/{Drawer,PatchTab,ErrorTab,ConflictTab,ExportTab,SettingsDialog}.test.tsx、viewState 扩展 | 组件单测 + seen 读写 |
| e2e/{host-rebase,host-submit,host-export}.spec.ts | 冲突经新页签(resubmit 双方改动)、submit-result 卡断言、导出走抽屉页签 |
| src/lumio_config/editor_static/ | 随卡重建 ×2,幂等 |

## ② 验证证据

- 门槛(终态):vitest **278/278**;全量 e2e **43/43**;python 141 OK;validate/format/diff-check 过;editor_static 幂等。
- 快审 + 复审(`.sdd/m6-i-review.md`):首轮 RETURN 2×P1(跳格行名映射、seen 修订源竞态),修复后复审 **MERGE**——跳格三类 op 实证落格、自家提交后 banner 空 + 回归断言绿。

## ③ known gaps(复审 P2,共 8 条)

- rename 组跳格:groupPatch name 为「old → new」复合串,永不命中(建议按 ops[index] 的 op.name/op.to 匹配)。
- vcs=none 时 session.revision 为 null,自家提交仅凭指纹差异可能出 seen 横幅(git 仓不受影响)。
- Drawer 开合未持久化到 viewState;E1 六类 / E2 部分 / TopBar·StatusBar 短文案仍为组件内常量(待 M6-J/Task 20 收口进 copy.ts);fixture 模式侧栏行数 0。
- 其余见复审报告。

## ④ 知识沉淀落点

- 接线排障记录全录台账(conflictKey 三段式、resolved 记录契约、resubmit 重建、host e2e 需重建静态包、seen 同源修订);无新决策。

## 附:Workflow 卡对账

- 卡号:R-00381。S01 → PatchTab 单测 + host-submit submit-result;S02 → ErrorTab 单测 + onJump 接线;S03 → ConflictTab 单测 + host-rebase 三例(含 resubmit 双方改动);S04 → ExportTab/SettingsDialog/viewState 单测 + host-export。
- 台账:`.sdd/progress.md`;审查 `.sdd/m6-i-review.md`(含复审节)。
- Owner 动作:R-00381 流转验收。

---

## 2026-09-04 审计对账补注(移档时追加,非原件内容)

来源:`docs/reviews/2026-09-04-editor-v3-completion-audit.md`(下称「审计」,本仓提交 472f3f8;本节行号均指该版),审计对象 main@f2b0ecf,取证口径为审计员亲自复跑。

- 审计 §A:Task 14(行 64)「完成」、Task 15(行 65)「完成(带缺陷)」、Task 16(行 66)「完成」——`Drawer`(7)/`PatchTab`(8)/`ErrorTab`(10)/`ConflictTab`(12)/`ExportTab`(6)/`SettingsDialog`(6) 单测绿;`host-submit`、`host-rebase` 3 条、`host-export` 绿;独立复核 `window.prompt` 0 命中;实走导出真生成 4 个 `export-link` 落盘 `build/export/editor/<id>/`。
- 审计 §B R-00381(行 94–97):S01「完成」、S02「完成(带缺陷)」、S03「完成」、S04「完成」。
- 审计为 S02 跳格补了真浏览器人工证据(审计 §C 第 5 步,行 119:红头卡「skills · fireball」+「damage · damage is required · provide a value · MISSING_REQUIRED」,点击跳格生效)。
- **审计新发现(收口后,原件 known gaps 未含,P2)**:还原改动回到 `ReadyClean`、补丁 0 之后,错误页签仍挂上一次预检的 1 条错误、等 6s 不消(审计 §E-2,行 265;实走 §C 第 8 步,行 122)——`App.tsx:1416-1425` 的 state 选择器把 `errors.length > 0` 排最前,`no-changes` 等空态被挡死;`setErrors([])` 只在草稿保存/提交成功时调用,回滚不触发。已立 M7-B 全量修复(需求卡 `.spec/plans/2026-09-04-editor-v3-hardening-requirements.md` 附录 B E-2)。
- 未复核:原件 ② 引用的 `.sdd/m6-i-review.md` 与首轮 RETURN 两条 P1 的复审细节审计未读取(其 §0.2 / §G-4 按 git 视角判定交回物缺失,本补注不为其背书);rename 组跳格等原 known gaps 未逐条复核,以原件记录为准。
