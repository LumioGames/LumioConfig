# M6-F(R-00378)交回物 · 2026-09-03

> **移档说明(2026-09-04)**:本文为 M6 收口时当场写下的原始交回物,因 `.sdd/` 整目录 gitignore(`*`)未曾入库;2026-09-04 按 M7-J(R-00402,Owner 闸门选项 A)从本地 `.sdd/` 移入 `docs/reviews/` 留档。原文内容未改写,仅文末追加「2026-09-04 审计对账补注」。原件提及的 `.sdd/` 台账(gate 日志、审查报告、diff 包)仍未入库,仅存撰写机器本地。

集成分支 `feat/editor-v3-m6-f`(基线 0fe95bc),合入 main 提交 `d7d1225`,已 push。

## ① 改动清单

| 文件 | 一句话 |
| --- | --- |
| editor/src/spreadsheet/interceptors.ts | P1-1:`attachLumioFromEdit` 见 `v` 变化即转 value;P1-2:无 range 的 CLEAR 取活动选区按 0-7 §5 逐格分派;mutation `{v:null}` 不记 `raw:""`;四态/CLEAR 写显式 `v:null` |
| editor/src/spreadsheet/projection.ts | P2-1:`buildCell` 写路径对空显示值显式 `v:null`(快照路径不变) |
| editor/src/spreadsheet/editors.ts | `numberOutOfRange` 加 `NUMBER_TYPES` 守卫,string/ref 列恒 false |
| editor/src/app/App.tsx | P2-2:open 动作经纯 reducer 预投影进 `stateRef` 再装拦截器 |
| editor/tests/{interceptors,projection.roundtrip,editors}.test.(ts|ts) | 单测 31 条(新增 17) |
| editor/tests/e2e/keyboard.spec.ts(新) | 真实键盘 T0–T5;T0 含无红字像素断言(zlib 解码 PNG,判据收紧到 #C5221F 核心笔画) |
| editor/docs/four-state-render-spike.md + editor/docs/spike/**(新) | spike 调研 + 最小 demo + 截图 |
| .spec/decisions/0008-four-state-rendering.md(新)+ README 索引 | 四态徽标采渲染扩展(CELL_CONTENT 拦截器 + customRender) |
| .spec/knowledge/lessons.md | 键盘提交携带旧 custom 的教训 |
| src/lumio_config/editor_static/ | 随卡重建(CI 可复现性,幂等复验 0 diff) |

## ② 验证证据

- 全量收口门槛(`.sdd/m6-f-gate.log`):lint+check-deps OK;vitest 14 文件/65 条;build OK;e2e 全套 **32 passed**;python unittest **136 OK**;validate OK;format --check OK;`git diff --check` 空;editor_static 重建后二次 build **0 diff**。
- 深审(对抗,`​.sdd/m6-f-review.md`,reviewer 指令 MERGE):四条验收项全部独立复验——S01 含三条反例路径实证(快照路径/同值幂等/粘贴重写 rowKey);S02 双格选区实测;S03 像素级验证(干净 0 红像素/阳性 24);S04 API 出处抽查 5 处属实、demo 实跑。无 P0/P1。
- 深审 F1(无红字断言缺失)已由主 loop 落地为 T0 像素断言(`e3c0cfb`),keyboard.spec 复跑 **6/6**。

## ③ known gaps

- F2(深审 P2):Ctrl+ 多选区 Delete 只清活动选区,非活动选区不派发。0-7 §5 未规定多选区语义,记为已知限制,后续卡如需再议。
- 观察项(深审):粘贴空值格坍缩语义、Ctrl+C 清 redo 栈(测试编排已规避)、T4/T5 只能在 fixtures 上跑(真仓三表无 default/可选列,形态不可达属预期,ADR 0006 已声明)。
- required 清空提示文案仍用 fourState.ts 旧文案(「required 列不能清空」),M6-G 接线换 COPY 时同卡改 T3 断言(台账已挂)。

## ④ 知识沉淀落点

- 决策:`.spec/decisions/0008-four-state-rendering.md`(新,0006 移交的决定权已行使)。
- 教训:`.spec/knowledge/lessons.md`(Univer 键盘提交 value 携带整格旧 custom;executeCommand 参数形状 ≠ 真实输入)。
- 其余为纯修复,豁免新增知识;无新模式引入(worktree 扇出 + 主 loop 接线为 ADR 0007 既有模式)。

## 附:Workflow 卡对账

- 卡号:R-00378(M6-F)。四条验收项证据指针:S01 → keyboard.spec T2 + interceptors.test (a);S02 → T3/T4/T5 + (b)(c)(d);S03 → T0(status-hint「就绪」+ 0 红像素)+ editors.test;S04 → four-state-render-spike.md + ADR 0008 + demo 截图。
- 台账:`.sdd/progress.md` 阵列 A 各行;深审报告 `.sdd/m6-f-review.md`。
- Owner 动作:把 R-00378 流转到验收(主 loop 不碰 Workflow 写接口)。

---

## 2026-09-04 审计对账补注(移档时追加,非原件内容)

来源:`docs/reviews/2026-09-04-editor-v3-completion-audit.md`(下称「审计」,本仓提交 472f3f8;本节行号均指该版),审计对象 main@f2b0ecf,取证口径为审计员亲自复跑。

- 审计 §A Task 1–4(行 51–54)均判「完成」:`interceptors.test` 17 条、`e2e/keyboard.spec.ts` T0–T5 六条全绿(真 `page.keyboard`,mutation `{v:null}` 守卫见 `interceptors.ts:125,181,197`);`projection.roundtrip` 19 条含 10k×50 fixture 回环、`editors.test` 5 条;`keyboard.spec.ts:247` T0 绿;ADR 0008、spike 文档与 demo 在位。
- 审计 §B R-00378(行 82–85):S01–S04 四项均「完成」。
- 审计 §0.1 终态门槛(与原件 ② 的当时数值时点不同,各自口径为准):e2e 54 passed(45.0s,含 axe 三态零违例)、Python 150 tests OK、validate / format --check 过。
- 未复核:原件 ② 引用的 `.sdd/m6-f-gate.log`、`.sdd/m6-f-review.md` 等本地产物,审计未读取(其 §0.2 / §G-4 按 git 视角判定交回物缺失,本补注不为其背书);原件 ③ known gaps 审计未逐条复核,以原件记录为准。
