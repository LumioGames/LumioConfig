# M6-J(R-00382)交回物 · 2026-09-03

> **移档说明(2026-09-04)**:本文为 M6 收口时当场写下的原始交回物,因 `.sdd/` 整目录 gitignore(`*`)未曾入库;2026-09-04 按 M7-J(R-00402,Owner 闸门选项 A)从本地 `.sdd/` 移入 `docs/reviews/` 留档。原文内容未改写,仅文末追加「2026-09-04 审计对账补注」。原件提及的 `.sdd/` 台账(评审报告、探针记录)仍未入库,仅存撰写机器本地。

- 卡:R-00382「快捷键旅程 / a11y / 阻断页」(M6-F 波次后第 4 张 Workflow 卡,Task 16/17 落点)
- 分支:`feat/editor-v3-m6-j` @ 1e3095a(5a00391 接线 + editor_static 重建 + 1e3095a 复审修复)
- 合入:main `cdacdc0`(merge --no-ff),已 push origin/main
- 评审:`.sdd/m6-j-review.md` —— 首轮 RETURN(P1-1 a11y 首开 toast 竞态 2/25 复现)→ 修复 1e3095a → 复审 **MERGE**(38 次 a11y 执行 + 25 次探针零失败)

## 改动清单(015ad04..1e3095a,21 文件 +2090/−166)

- `src/panels/CommandPalette.tsx`(273 行,Ctrl+K 命令面板,aria-activedescendant)
- `src/panels/SubmitConfirm.tsx`(提交确认,仅 autoCommit||autoExport)
- `src/panels/ShortcutsDialog.tsx`(快捷键表对话框)
- `src/panels/Blocked.tsx`(整页阻断页 alertdialog + 两步重连指引)
- `src/components/ui/useHotkeys.ts`(+93:worksInGrid 标志、大小写归一、MAC_MODIFIERS)
- `src/app/App.tsx`(+226:全局热键捕获 / 命令面板 / 提交确认 / 阻断页挂载)
- `tests/`:CommandPalette 12 例、SubmitConfirm 6 例、Blocked 9 例、useHotkeys 8 例;
  e2e `keyboard-journeys.spec.ts`(J2/J4/J5 + 无确认反向例)、`a11y.spec.ts`(axe 三态)
- devDeps 仅新增 `@axe-core/playwright@4.13.0`;editor_static 重建随附

## 验证证据

- 主 loop 门槛(5a00391):vitest 313/313;e2e 54/54(含 host 系对静态包);
  python 141 例;lint/format/validate/git diff --check 干净
- 评审必跑(1e3095a detached worktree):vitest 四文件 35/35;journeys+a11y 原失败配置 ×8 轮全绿;
  a11y --repeat-each=10 → 30/30;根因探针 ×25 → toast 0/25、serious+ 0/25;全量 e2e 50 passed
- 硬约束面:8 保留 testid 在位、6 中文断言原样、无字面色(tokens 守卫过)、ADR/schemas/tables/registry 未动

## Known gaps(评审 P3 ×4,已记档不阻断)

1. keyboard-journeys 新增 2 处中文断言 + 1 处「导出」查询串耦合,§12 文案变更需同卡改
2. 应用级捕获热键只豁免 INPUT/TEXTAREA:对话框开着且焦点在 BUTTON 上时 Ctrl+B/K/S 穿透;
   命令面板聚焦时 Ctrl+K 不能二次关(Esc 可关)
3. Drawer tabpanel 未与 tab 建 aria-labelledby(axe 不再报,增强项)
4. Blocked 若 kind 原地切换(offline→closed)不重新移焦(当前挂载条件保证一次一实例)

## 沉淀

- a11y 竞态根因模式:一次性 onboarding toast 的 150ms 渐入与 axe 快照竞态;
  修法 = 测试上下文预置 localStorage 键移除载体,而非永久豁免 `.toast`
- Univer 焦点模型:画布 contenteditable false-positive → 捕获阶段监听 + 只豁免真文本输入;
  应用级热键与表格级热键经 worksInGrid 分流
- 「Univer 吞 Tab」下的 J5 校准口径:ExportTab 挂载自动聚焦 btn-export(仅移焦不点击)

## 卡对账(R-00382 验收口径)

- S01 键盘旅程 J2/J4/J5 自动化 ✅(J1 切表段并入 J2、J3 预检段并入 J2,独立用例记残留)
- S02 提交确认仅副作用时弹出 ✅(反向用例 journeys:306)
- S03 a11y 三态扫描 serious=0 ✅(后续 Task 20 将 moderate 残留也清零,见 m6-k 交回物)
- S04 文档归 Task 20 ✅(docs/reference/editor.md v3 段 + 快捷键表已随 m6-k 交付)

---

## 2026-09-04 审计对账补注(移档时追加,非原件内容)

来源:`docs/reviews/2026-09-04-editor-v3-completion-audit.md`(下称「审计」,本仓提交 472f3f8;本节行号均指该版),审计对象 main@f2b0ecf,取证口径为审计员亲自复跑。

- 审计 §A:Task 17(行 67)「完成」——`CommandPalette`(12)/`SubmitConfirm`(6) 单测绿、`keyboard-journeys` 4 条绿、`Ctrl+K` 实走三组 13 条命令、快捷键标签正确;Task 18(行 68)「**部分**」——`Blocked` 9 单测绿、`e2e/a11y.spec.ts` 三态 axe `violations: []` 绿、`a11y-checklist.md` 在位且诚实标注 3 条未勾,但 `Blocked` 在真实掉线场景不可达。
- 审计 §B R-00382(行 98–101):S01「完成」、S02「完成」(`keyboard-journeys.spec.ts:306` 反向用例)、S03「**部分**」、S04「完成」(Task 20 交付)。
- **审计新发现(收口后,P1,原件未含)**:阻断页不可达——全前端无一处派发 `online:false`(`App.tsx:536,794` 只派发 true;SSE 断流在 `client.ts:88-90` 只是静默 `break`;`draftSession.ts:51-56` 的 `subscribeEvents(...)` 无 `.catch`),`App.tsx:1745` 的 `hostMode && !state.online` 只对「首次就没连上」成立(审计 §E-1,行 264;实走 §C 第 10 步,行 124)。同场景还观测到:杀 Host 后 25s 界面仍「在线」、再切表落 `Failed` 且胶囊显示「提交失败」(用户从未提交,审计 §G-5)、一次复现整页白屏未捕获异常。原件 S03 只把该缺口记到「截图没补」级别,审计判定为 R-00382 S03 的实质功能缺口。已立 M7-A 全量修复(需求卡 `.spec/plans/2026-09-04-editor-v3-hardening-requirements.md` 附录 B E-1)。
- a11y 清单勾选口径漂移(审计 §G-3:§3 第 4 条 J1–J5 与第 6 条错误跳格打 `[x]` 但正文写「记为残留」)由 M7-J(R-00402)S04 修正为 `[~]`。
- 未复核:原件 ② 的 38 次 a11y 执行、25 次探针等评审数值审计未读取(其 §0.2 / §G-4 按 git 视角判定交回物缺失,本补注不为其背书);评审 P3 ×4 未逐条复核,以原件记录为准。
